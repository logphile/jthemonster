// Disabled copy: do not use
// Keeping this for historical reference. The real client is provided by @nuxtjs/supabase
// via `useSupabaseClient()` and `useSupabaseUser()`.
//
// If you need helpers, create a different-named composable and call:
//   const supabase = useSupabaseClient()
//   const user = useSupabaseUser()
